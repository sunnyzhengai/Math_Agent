[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --strict-markers
    --tb=short
    -ra
markers =
    golden: golden snapshot tests
    deterministic: tests requiring deterministic seeds
    integration: integration tests
    unit: unit tests
